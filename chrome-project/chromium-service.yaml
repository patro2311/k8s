apiVersion: v1
kind: Service
metadata:
  name: chromium-service
spec:
  selector:
    app: chromium
  ports:
    - protocol: TCP
      port: 7001
      targetPort: 7001 # container define port 
      nodePort: 30100
  type: NodePort