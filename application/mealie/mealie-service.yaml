apiVersion: v1
kind: Service
metadata:
  labels:
    app: mealier_tag
  name: mealier-deployment
spec:
  ports:
  - port: 8008
    protocol: TCP
    targetPort: 9000
  selector:
    app: mealier_tag
  type: LoadBalancer # Now you can connect to the IP of the node + :9000 port
        
